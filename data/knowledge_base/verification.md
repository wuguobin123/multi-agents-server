# 知识库链路验证说明

## 验证目标

这份文档用于验证知识库新增内容是否已经完成入库、切块、向量化和问答检索全链路。

## 验证知识

本次新增知识的唯一验证口令是 `蓝莓管道-20260315`。

如果问“知识库新增的链路验证口令是什么”，正确命中时应返回 `蓝莓管道-20260315`，并引用 `data/knowledge_base/verification.md`。

## 验证步骤

1. 在知识库目录新增或更新文档。
2. 执行索引重建脚本刷新向量库。
3. 通过 `/v1/chat` 发起包含“知识库”和“链路验证口令”的问题。
4. 检查返回答案和 citations，确认命中新文档。
